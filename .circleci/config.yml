version: 2.1

orbs:
  general-platform-helpers: okta/general-platform-helpers@1.8.13

aliases:

  - &build_steps
    - checkout
    - run: java -version
    - run: ./mvnw -ntp clean install -Pci 

jobs:
  
  jdk17:
    docker:
      - image: cimg/openjdk:17.0.6
    environment:
      JVM_OPTS: -Xmx3200m
    steps: *build_steps

  snyk-scan:
    docker:
      - image: cimg/python:3.10
    steps:
      - platform-helpers-general/step-load-dependencies
      - platform-helpers-general/step-run-snyk-monitor:
          scan-all-projects: true
          skip-unresolved: false
    
workflows:
  build_and_test:
    jobs:
      - jdk17
  # See OKTA-634437
  semgrep:
    jobs:
      - general-platform-helpers/job-semgrep-prepare:
          name: semgrep-prepare
      - general-platform-helpers/job-semgrep-scan:
          name: "Scan with Semgrep"
          requires:
            - semgrep-prepare
  snyk-dependency-scan:
    jobs:
      - platform-helpers-general/job-snyk-prepare:
          name: prepare-snyk
          requires:
            - build
      - snyk-scan:
          name: execute-snyk
          requires:
            - prepare-snyk